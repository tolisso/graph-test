# GraphML Visualizer Frontend

React + TypeScript фронтенд для визуализации GraphML файлов с использованием React Flow.

## Требования

- Node.js 16+
- npm или yarn

## Установка

Зависимости уже установлены. Если нужно переустановить:

```bash
npm install
```

## Запуск

### Development режим

```bash
npm run dev
```

Приложение запустится на `http://localhost:5173`

### Production build

```bash
npm run build
npm run preview
```

## Использование

1. Запустите бэкенд сервер на порту 8080
2. Откройте фронтенд в браузере
3. Нажмите "Choose GraphML file" и выберите .graphml файл
4. Граф автоматически отобразится на экране

## Возможности

- 📁 Загрузка GraphML файлов
- 📊 Интерактивная визуализация графа
- 🎨 Автоматическое расположение узлов по кругу
- 🔍 Zoom и Pan
- 🎯 Перетаскивание узлов
- 📝 Отображение меток на узлах и рёбрах

## Структура проекта

```
src/
├── components/
│   ├── FileUploader.tsx       - Компонент загрузки файла
│   ├── FileUploader.css
│   ├── GraphVisualization.tsx - Компонент визуализации
│   └── GraphVisualization.css
├── types.ts                   - TypeScript типы
├── App.tsx                    - Главный компонент
├── App.css
├── index.css                  - Глобальные стили
└── main.tsx                   - Точка входа
```

## Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Build tool и dev server
- **React Flow** - Библиотека для визуализации графов
- **CSS3** - Стилизация

## API

Фронтенд взаимодействует с бэкендом через единственный endpoint:

```
POST http://localhost:8080/parse
Content-Type: application/json

{
  "graphml": "<graphml>...</graphml>"
}
```

Response:
```json
{
  "nodes": [
    { "id": "node1", "label": "Node Label" }
  ],
  "edges": [
    {
      "id": "edge1",
      "label": "Edge Label",
      "source": "node1",
      "target": "node2",
      "pair": "node1 -> node2"
    }
  ]
}
```
